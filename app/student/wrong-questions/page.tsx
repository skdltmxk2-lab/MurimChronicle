import { WrongQuestionsClient } from "@/components/student/WrongQuestionsClient";

export const metadata = {
  title: "최근 틀린 문제 · 루트매쓰 CBT",
};

export default function WrongQuestionsPage() {
  return <WrongQuestionsClient />;
}
